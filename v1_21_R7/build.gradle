plugins {
    id "io.github.patrick.remapper" version "1.4.2-custom"
}

dependencies {
    // https://hub.spigotmc.org/stash/projects/SPIGOT/repos/spigot
    implementation "org.spigotmc:spigot:$remapMcVersion:remapped-mojang"

    implementation project(':core')
}

tasks {
    assemble {
        dependsOn('remap')
    }

    remap {
        version.set(remapMcVersion)
    }
}
