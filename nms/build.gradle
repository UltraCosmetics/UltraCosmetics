dependencies {
    // https://hub.spigotmc.org/stash/projects/SPIGOT/repos/spigot
    // Spigot 26.x publishes a Mojang-mapped jar directly (no remapper needed).
    implementation "org.spigotmc:spigot:$remapMcVersion"

    implementation project(':core')
}
